# Claude Code Custom Commands

Эта директория содержит пользовательские slash команды для VA-PC Telegram Mini App проекта.

## Как использовать

В Claude Code введите `/` чтобы увидеть все доступные команды. Команды из этой папки будут отображаться в списке.

## Доступные команды

### 🔧 Разработка и рефакторинг

#### `/fix-todo`
Находит все TODO комментарии в коде и предлагает их исправление.
- Приоритизирует по важности
- Фокус на API, UI, производительности и безопасности

#### `/refactor-component`
Рефакторит React компонент следуя best practices.
```
/refactor-component FPSMetricsDisplay
```
- Улучшает производительность
- Добавляет accessibility
- Оптимизирует типизацию

### ✅ Тестирование

#### `/improve-tests`
Анализирует кодовую базу и рекомендует области для улучшения тестового покрытия.
- API routes тесты
- Компоненты тесты
- Integration тесты
- Примеры с Vitest/Jest

#### `/check-types`
Проверяет TypeScript типы и предлагает улучшения.
- Находит `any` типы
- Проверяет Prisma типы
- Улучшает type safety

### 🎮 FPS Метрики

#### `/add-fps-game`
Добавляет новую игру в систему FPS метрик.
```
/add-fps-game "The Witcher 3"
```
- Обновляет validation
- Добавляет иконку
- Обновляет документацию

### 🔒 Безопасность

#### `/review-security`
Проводит аудит безопасности кодовой базы.
- Authentication & Authorization
- Input validation
- API security
- Telegram Mini App security
- Dependencies vulnerabilities

### 🗄️ База данных

#### `/optimize-db`
Анализирует и оптимизирует запросы к БД.
- Находит N+1 проблемы
- Предлагает индексы
- Проверяет Prisma queries
- Рекомендует кэширование

#### `/generate-migration`
Генерирует и проверяет Prisma миграцию.
```
/generate-migration "add user preferences table"
```
- Обновляет schema
- Генерирует миграцию
- Проверяет SQL
- Обновляет типы

### 📦 Оптимизация

#### `/analyze-bundle`
Анализирует размер Next.js bundle и предлагает оптимизации.
- Bundle analysis
- Code splitting
- Lazy loading
- Package optimization

### ⚙️ Конфигурация

#### `/setup-env`
Настраивает environment конфигурацию для разных окружений.
- Development, Production, Test
- Validation скрипты
- .env.example

## Создание своих команд

1. Создайте `.md` файл в этой директории
2. Имя файла = имя команды (без .md)
3. Содержимое = промпт для Claude

Пример:
```bash
echo "Analyze this code for bugs" > .claude/commands/find-bugs.md
```

Теперь команда `/find-bugs` доступна в Claude Code!

## Параметры

Используйте `$ARGUMENTS` в файле команды для передачи параметров:

```markdown
Generate unit tests for: $ARGUMENTS
```

Использование:
```
/your-command ComponentName
```

---

Создано для VA-PC Telegram Mini App проекта 🚀
